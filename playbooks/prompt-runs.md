# Prompt Runs Playbook

Use three prompt layers:

- `broad` for non-branded discovery
- `narrow` for proof-legibility
- `branded control` for identity retrieval and grounding

For each run, record:

- whether the founder or site is mentioned
- whether the site is cited
- whether the intended point of view survives
- where retrieval or grounding breaks

